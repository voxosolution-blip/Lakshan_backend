{
  "name": "lakshan-backend",
  "version": "1.0.0",
  "description": "Yoghurt Factory ERP Backend API",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "cd backend && node server.js",
    "start:docker": "cd backend && node server.js",
    "build": "cd backend && npm install",
    "postinstall": "cd backend && npm install"
  }
}
