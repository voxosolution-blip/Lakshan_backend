{
  "name": "lakshan-backend",
  "version": "1.0.0",
  "description": "Yoghurt Factory ERP Backend API",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "build": "cd backend && npm install"
  }
}
